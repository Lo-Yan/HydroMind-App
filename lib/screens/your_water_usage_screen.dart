import 'package:flutter/material.dart';


import 'package:flutter/material.dart';

class YourWaterUsageScreen extends StatelessWidget {
  const YourWaterUsageScreen({Key? key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Your Water Usage Screen'),
      ),
      body: Center(
        child: const Text('This is the your water usage screen.'),
      ),
    );
  }
}
